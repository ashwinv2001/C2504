public CheckInViewModel()
    {
        // Initialize repository or other services here with default values, if possible
        _checkInsRepo = new CheckInsRepo(new MatDbEntities()); // Example of initializing with a default repository
        LoadMedications();
    }
