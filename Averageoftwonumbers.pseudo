using System.Collections.ObjectModel;

namespace BankManagementSystem.Databases
{
    public interface IAccountRepo
    {
        ObservableCollection<Account> ReadAll();
        void Add(Account account);
        void Update(Account account);
        void Delete(Account account);
        void Deposit(int accountNumber, int amount); // Add Deposit method
        void Withdraw(int accountNumber, int amount); // Add Withdraw method
        void CalculateInterestAndUpdateBalance();
    }
}
