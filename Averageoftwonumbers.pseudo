public XmlNodeViewModel(XElement element)
{
  //  MessageBox.Show("This is inside XmlNodeViewModel");
    XmlNode = element;
    Name = element.Name.LocalName;

    foreach (var child in element.Elements())
    {
        Children.Add(new XmlNodeViewModel(child));
    }
}
