@echo off
set APP_NAME=Microsoft.WindowsCalculator_8wekyb3d8bbwe!App
set PROCESS_NAME=CalculatorApp.exe

:: Launch the UWP app
start shell:AppsFolder\%APP_NAME%

:: Wait for 10 seconds
timeout /t 10 >nul

:: Close the UWP app
taskkill /F /IM %PROCESS_NAME%
