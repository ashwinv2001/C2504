private void textBoxAccountHolder_TextChanged(object sender, TextChangedEventArgs e)
        {
            if (string.IsNullOrEmpty(textBoxAccountHolder.Text))
            {
                placeholderTextBlock.Visibility = Visibility.Visible;
            }
            else
            {
                placeholderTextBlock.Visibility = Visibility.Hidden;
            }
        }
